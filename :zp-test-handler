emulate -RL zsh
setopt extendedglob warncreateglobal typesetsilent noshortloops

[[ "$1" = plugin ]] && \
    local type="$1" user="$2" plugin="$3" id_as="$4" dir="$5" || \
    local type="$1" url="$2" id_as="$3" dir="$4"

(
    cd $dir
    integer quiet=0
    zstyle -T ":z-plugin:test" quiet && quiet=1

    if [[ -f Makefile ]]; then
        …zp-test-make-targets Makefile
        if (( ${reply[(I)test]} )); then
            print -P -- "%F{38}test z-plugin: %F{154}RUNNING MAKEFILE TESTS"
            if (( quiet )); then
                if make test &>/dev/null; then
                    print -P -- "%F{38}test z-plugin: %F{154}The tests succeeded"
                else
                    print -P -- "%F{38}test z-plugin: %F{154}The tests have failed"
                fi
            else
                make test
            fi
        fi
    fi
    local -a farray
    farray=( **/*.zunit(N) )
    if (( ${#farray} )); then
        if type zunit >/dev/null 2>&1; then
            print -P -- "%F{38}test z-plugin: %F{154}RUNNING ZUNIT TESTS"
            if (( quiet )); then
                if zunit &>/dev/null; then
                    print -P -- "%F{38}test z-plugin: %F{154}The tests succeeded"
                else
                    print -P -- "%F{38}test z-plugin: %F{154}The tests have failed"
                fi
            else
                zunit
            fi
        else
            print -P -- "%F{38}test z-plugin: %F{154}Please install molovo/zunit to" \
                   " run the zunit tests provided with the plugin%f"
        fi
    fi

)

# vim:ft=zsh:sw=4:sts=4:et
